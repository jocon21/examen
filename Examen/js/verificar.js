function go(){
    if (document.formingreso.usuario.value=='maria' && document.formingreso.contraseña.value=='examen');
    {
        document.formingreso.subwit ();
    }
    else
    {
        alert("Ingrese su Usuario y Contraseña correctamente");
    }
}

